---
platform: linux

params:
  ADMIN_USER:
  VERSION:
  PACKAGING:

inputs:
  #- name: reserved-job-git
  - name: ci3-git
  - name: design-git

outputs:
  - name: archive-out

run:
  path: ci3-git/ci/archive.sh
